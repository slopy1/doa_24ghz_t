# DOA-24GHz — host PC requirements (Path A in INSTALL_GUIDE.md)
# Install with: pip install -r requirements.txt
#
# These cover offline analysis (replaying captured CSVs, regenerating plots).
# The KV260 deployment (Path B) installs SoapySDR / libbladeRF / SoapyBladeRF
# and python3-libgpiod from system packages or source — see INSTALL_GUIDE.md.

numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0

# Optional: enable if you keep large IQ datasets in HDF5 instead of CSV
# h5py>=3.8.0
